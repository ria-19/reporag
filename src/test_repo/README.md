# Test Repository

This is a simple test repository for our RAG ingestion pipeline.

## Purpose

The purpose of this repository is to provide a controlled environment to test the `GitHubRepoLoader` and the `CodeAwareChunker`. It contains a Python file and this README.

## Usage

Run the main processing script to see how these files are loaded and chunked.